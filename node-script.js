/**
 *  This is a nodeJS script in the sense that we won't be loading
 *  it in an HTML page. Purely for testing purposes, go ahead and
 *  run this file once the Code Runner extension is installed.
 */

'use strict';

function main() {
    console.log("\nThe future is now. Codespaces changes everything.\n");

    for (let x = 0; x < 5; x++) {
        console.log("No go code something!");
    }
}

main();